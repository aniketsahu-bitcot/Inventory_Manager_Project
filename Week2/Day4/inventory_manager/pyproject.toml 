[project]
name = "inventory_manager"
version = "0.1.0"
description = "A simple inventory management project using Product and Inventory classes"
authors = [
    { name = "Aniket Sahu" }
]
readme = "README.md"
requires-python = ">=3.10"

# Project dependencies
dependencies = [
    "pydantic>=2.0.0",
    "python-dotenv>=1.0",
]